create table if not exists Weather(City_name varchar(20),
 Description varchar(20),
  Temperature float,
   Feels_like_temperature float,
    Min_temperature float,
    Max_temperature float,
     Humidity float,
      Clouds float);